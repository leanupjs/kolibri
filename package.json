{
	"name": "root",
	"private": true,
	"scripts": {
		"clean": "lerna exec --stream -- git clean -f -d -X && git clean -f -d -X",
		"format": "lerna exec --stream -- prettier --check src/**/* -w",
		"ncu:major": "ncu && lerna exec --concurrency=1 --stream -- ncu",
		"ncu:minor": "ncu -t minor -u && lerna exec --concurrency=1 --stream -- ncu -t minor -u",
		"pack": "lerna exec --concurrency=1 --stream -- npm pack",
		"postinstall": "lerna exec --stream -- pnpm :install",
		"prepare": "rimraf .husky && husky install && husky add .husky/commit-msg \"npx commitlint --edit $1\" && husky add .husky/pre-commit \"npx lerna run lint\" && husky add .husky/pre-commit \"npx lerna run format\"",
		"publish": "lerna exec --stream -- pnpm publish",
		":publish": "lerna exec --stream -- pnpm :publish",
		"update": "npm run ncu:minor && pnpm i && npm run ncu:major"
	},
	"devDependencies": {
		"@commitlint/cli": "17.0.3",
		"@commitlint/config-conventional": "17.0.3",
		"cross-env": "7.0.3",
		"husky": "8.0.1",
		"lerna": "5.1.8",
		"npm-check-updates": "15.3.3",
		"prettier": "2.7.1",
		"rimraf": "3.0.2"
	},
	"engines": {
		"npm": ">=8",
		"pnpm": ">=3"
	}
}
